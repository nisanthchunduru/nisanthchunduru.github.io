version: '3.8'

services:
  hugo:
    build: .
    volumes:
      - ./content:/opt/blog/content
    ports:
      - "1313:1313"
  hugo-notion:
    build:
      context: .
      dockerfile: Dockerfile.hugo-notion
    volumes:
      - ./content:/opt/blog/content
    env_file: .env
